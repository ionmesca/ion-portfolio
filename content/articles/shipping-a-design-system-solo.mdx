---
title: "Shipping a design system solo"
date: "2025-11-18"
summary: "One person cannot govern a system. One person can make every decision twice — once in Figma, once in the stylesheet — and keep the two honest."
---

PLACEHOLDER ARTICLE. Filler text throughout. It exists so /articles has a
second year group, which is what gives the index's wheel more than one row and
the year headers something to separate.

## Two files, one decision

Filler. A paragraph at the prose step, long enough to wrap several times inside
the 640 column so the measure can be judged rather than guessed at.

Filler, second paragraph. The heading above is `text-lg`, the same step the
letter uses, because the article template inherits the letter and does not get
a type scale of its own.

<Photo caption="Placeholder media slot — one slot, full width of the column." />

## The check that has to fail

Filler. A middle section, so the wheel has three rows on this page and the lens
ladder can be seen falling off rather than merely being described.

Filler, second paragraph, with a `code span` in it so the inline treatment is
exercised somewhere. Fenced code blocks are deliberately absent: syntax
highlighting is not wired yet, and an unstyled block would read as a bug.

## What survives

Filler. The closing section, and the last thing on the page before the
footnote rule.
