---
title: "The wheel is the nav"
date: "2026-03-12"
summary: "A list of links tells you where you can go. A wheel tells you where you are, which is the question people actually have."
---

PLACEHOLDER ARTICLE. Every paragraph below is filler, written to give the
article template a real shape to be judged against — the measure, the rhythm
between headings, the rail's wheel with something to track. Ion writes the real
essays post-build. Do not quote any of this.

This one is deliberately the LONG article of the three, so the page clears the
conditional wheel's threshold and the rail's section list appears. Its short
sibling, "Sound in interfaces, quietly", deliberately does not.

## Where the argument starts

Filler. A paragraph of the right length to test the 640 column and the 16/170
prose step, and nothing more. The sentences are grammatical and the rhythm is
plausible, which is all a placeholder owes the layout it is standing in for.

Filler, second paragraph. The gap between these two is the letter's 24, and the
gap up to the heading above is the letter's 16. If either looks wrong on this
page it looks wrong on the letter too, because they are the same numbers.

<PhotoRow>
  <Photo caption="Placeholder media slot — art lands post-build." />
  <Photo caption="Placeholder media slot — the pair tests the 16px gutter." />
</PhotoRow>

## The part that is usually skipped

Filler. Somewhere around here a real article would earn its length. This one
just occupies it, at the same density, so that scrolling this page feels like
scrolling a page rather than like scrolling a stub.

Filler, again. A second paragraph, because one-paragraph sections make the
wheel's handoff windows unrealistically short and the rail would be tuned
against a page nobody will ever read.

- A list item, to prove the marker sits in the gutter and not in the measure.
- A second item, longer than the first, so the wrap lands somewhere visible and
  the leading between wrapped lines can be checked against the prose step.
- A third, short.

## What it costs

Filler. The cost paragraph. A real version of this section would have a number
in it and an argument about whether the number was worth it.

> A pull quote, placeholder. It exists to show the rule on the left and the
> muted foreground, which is the only quote treatment the template has.

Filler after the quote, so the spacing below a blockquote is exercised as well
as the spacing above it.

## The objection

Filler. Every essay has the paragraph where it argues with itself, and the
template should be able to hold one without the reader losing the thread.

Filler, continued. This section is intentionally the longest, because a page
whose sections are all the same height is not a test of a scroll-linked wheel —
it is a test of a ruler.

Filler, third paragraph. The wheel's `handoffShare` option exists precisely
because sections are not evenly spaced, and a page of even sections would never
show whether it works.

Filler, fourth paragraph. By now the page is comfortably past one and a half
viewports on a laptop, which is the whole point of this file.

## What is actually true

Filler. The closing section, shorter than the others, which is how closings
tend to go and is also the case the wheel's end-pull guard was written for: a
last section too short to ever cross the 40% line.
